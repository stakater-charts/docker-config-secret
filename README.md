# chart-docker-config-secret
This repository contains 1 chart that is used to deploy docker-config-secret to kubernetes.
- docker-config-secret

## Installing
Install `docker-config-secret` chart
```
helm install --name docker-config-secret chartmuseum/docker-config-secret
```
